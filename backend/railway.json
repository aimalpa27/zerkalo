{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"python manage.py collectstatic --noinput && python manage.py migrate && python manage.py seed_plans && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}